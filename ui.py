import pygame
"""
This is where the window is supposed to be drawn.... (I hope!)
"""

def draw_window():
    screenWidth = 400
    screenHeight = 700
    screen = pygame.display.set_mode(screenWidth, screenHeight)

draw_window()
